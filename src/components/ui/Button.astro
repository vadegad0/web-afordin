---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'button'> {}

const { class: classNames, ...restProps } = Astro.props
---

<button class:list={['btn', classNames]} {...restProps}>
  <slot />
</button>
