---
import { languages } from '@/i18n/ui.ts'
---

<ul class="flex flex-row gap-2">
  {
    Object.entries(languages).map(([lang, label]) => (
      <li>{lang === 'es' ? <a href={`/`}>{label}</a> : <a href={`/${lang}`}>{label}</a>}</li>
    ))
  }
</ul>
